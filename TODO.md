### TODO:
- 根据目标仓库的git diff更新向量库和bm25库
- 知识图谱

### 顺序

3. git diff 增量更新索引 第三，因为它适合直接作为 MCP 的一个核心能力来设计。你可
以在 MCP 里把它定义成明确工具，例如“获取 diff”“删除旧 chunk”“重建受影响文件
chunk”“更新 BM25”。这样做出来的不是脚本能力，而是服务能力。反过来如果现在先在
单体里做，后面还是得重新整理到 MCP 接口里。


4. 知识图谱 仍然最后。它最依赖前面三项稳定：
   - 要依赖 MCP 的工具边界，决定图谱构建和查询是不是也服务化。
   - 要依赖沙箱，决定允许扫描哪些仓库内容。
   - 要依赖增量索引链路，决定图谱如何按变更增量更新，而不是每次全量重建。


3. 然后把“按 git diff 更新 Chroma/BM25”做成 MCP 的正式工具。
4. 最后把知识图谱作为新的检索/分析能力接进 MCP。
