### TODO:
- mcp服务的搭建
- 沙箱
- 根据目标仓库的git diff更新向量库和bm25库
- 知识图谱

### 顺序
1. MCP 服务 先做，因为你要先确定工具边界怎么拆。现在项目里工具是直接嵌在 Agent 里
的，核心在 agent/tools/*，并且通过 services/service_manager.py 注入向量库、
BM25、LLM、配置。如果后面要把它们独立成 MCP server，接口模型、入参出参、状态管
理、依赖注入方式都会变。这个边界不先定，后面的增量索引和图谱能力都可能做在旧抽
象上，之后还得再搬一次。


2. 沙箱 第二，因为一旦工具脱离本进程、变成服务，对外暴露面立刻扩大。当前仓库只有
agent/security.py 这类“校验型安全”，更像访问控制，不是完整隔离。MCP 化之后，文
件读取、代码搜索、命令执行、索引更新这些能力都需要服务端边界约束，所以安全模型
应当紧跟在服务拆分后落实，而不是最后补。


3. git diff 增量更新索引 第三，因为它适合直接作为 MCP 的一个核心能力来设计。你可
以在 MCP 里把它定义成明确工具，例如“获取 diff”“删除旧 chunk”“重建受影响文件
chunk”“更新 BM25”。这样做出来的不是脚本能力，而是服务能力。反过来如果现在先在
单体里做，后面还是得重新整理到 MCP 接口里。


4. 知识图谱 仍然最后。它最依赖前面三项稳定：
   - 要依赖 MCP 的工具边界，决定图谱构建和查询是不是也服务化。
   - 要依赖沙箱，决定允许扫描哪些仓库内容。
   - 要依赖增量索引链路，决定图谱如何按变更增量更新，而不是每次全量重建。


如果按实施视角再压缩一下，就是：

1. 先把现有 ReadFile、SearchCode、RunCommand、RetrieveAndSummarize 这些工具能
 力抽象成 MCP server 的接口。
2. 再补服务级沙箱，不再只依赖 Agent 内部白名单。
3. 然后把“按 git diff 更新 Chroma/BM25”做成 MCP 的正式工具。
4. 最后把知识图谱作为新的检索/分析能力接进 MCP。
