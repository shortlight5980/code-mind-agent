# 05. 完整集成与测试

在本文档中，我们将学习如何将 Agent 集成到 FastAPI 应用中，以及如何进行完整的测试！

---

## 一、config.yml 中的 Agent 配置

首先，让我们看看配置文件中新增的 Agent 相关配置：

```yaml
chroma:
  persist_dir: "./chroma_db"
  chunk_size: 800
  chunk_overlap: 100
  retrieval_k: 7

embeddings:
  model: "text-embedding-v3"

llm:
  model: "qwen-max"
  temperature: 0.1

summarizer:
  model: "qwen-max"
  temperature: 0.1

splitting:
  max_class_length: 3000

agent:
  enabled: true                        # 是否启用 Agent
  model: "qwen-max"                    # Agent 使用的模型
  temperature: 0.1                      # Agent 的温度参数
  allowed_dirs: ["."]                   # 允许访问的目录白名单
  blocked_files: [".env", "*.key", "*.pem", "*.p12", "id_rsa"]  # 禁止访问的文件
  allowed_commands: ["ls", "cat", "grep", "git", "find", "head", "tail", "wc"]  # 允许执行的命令
  command_timeout: 5                     # 命令执行超时时间（秒）
  max_search_results: 50                 # 搜索结果上限
```

### 配置说明

| 配置项 | 说明 |
|--------|------|
| `agent.enabled` | 开关，可以随时关闭 Agent，回退到普通 RAG 模式 |
| `agent.allowed_dirs` | 路径白名单，Agent 只能访问这些目录下的文件 |
| `agent.blocked_files` | 敏感文件黑名单，即使在白名单目录内也禁止读取 |
| `agent.allowed_commands` | 命令白名单，只允许执行这些命令 |
| `agent.command_timeout` | 防止命令卡住 |
| `agent.max_search_results` | 防止搜索结果太多导致输出过长 |

---

## 二、app.py 中的 Agent 集成

让我们看看如何在 FastAPI 应用中集成 Agent！

### 1. 服务初始化：init_services()

```python
def init_services():
    """Initialize vector database and LLM services."""
    Config.load()

    # 读取配置
    persist_dir = Config.get("chroma.persist_dir", "./chroma_db")
    retrieval_k = Config.get("chroma.retrieval_k", 7)
    embedding_model = Config.get("embeddings.model", "text-embedding-v3")
    llm_model = Config.get("llm.model", "qwen-max")
    llm_temperature = Config.get("llm.temperature", 0.1)
    summarizer_model = Config.get("summarizer.model", "qwen-max")
    summarizer_temperature = Config.get("summarizer.temperature", 0.1)
    agent_enabled = Config.get("agent.enabled", True)

    logger.info(f"Loading vector database: {persist_dir}")

    # 初始化嵌入模型
    embeddings = DashScopeEmbeddings(
        model=embedding_model,
    )

    # 加载向量数据库
    vectordb = Chroma(
        persist_directory=persist_dir,
        embedding_function=embeddings
    )

    # 初始化主 LLM
    llm = ChatTongyi(
        model=llm_model,
        temperature=llm_temperature,
    )

    # 初始化总结 LLM
    summarizer_llm = create_summarizer(
        model=summarizer_model,
        temperature=summarizer_temperature
    )

    # 存入全局字典
    services["vectordb"] = vectordb
    services["llm"] = llm
    services["summarizer_llm"] = summarizer_llm
    services["retrieval_k"] = retrieval_k

    # 初始化 Agent（使用新的 CodeMindAgent 类）
    if agent_enabled:
        logger.info("Initializing CodeMind Agent...")
        try:
            agent = CodeMindAgent()
            services["agent"] = agent
            services["agent_enabled"] = True
            logger.info("CodeMind Agent initialized successfully")
        except Exception as e:
            logger.error(f"Failed to initialize Agent: {e}")
            services["agent_enabled"] = False
    else:
        services["agent_enabled"] = False
        logger.info("Agent is disabled in config")

    logger.info("Services initialized successfully")
```

**关键点**：
- 读取 `agent.enabled` 配置
- 如果启用，使用 `CodeMindAgent()` 构造函数创建 Agent
- 如果初始化失败，自动禁用 Agent，继续启动服务（fail-open 设计）

### 2. 新增：流式输出生成函数

```python
async def generate_stream(
    agent: CodeMindAgent,
    question: str,
    docs: list,
    summarizer_llm: ChatTongyi
) -> AsyncGenerator[str, None]:
    """
    生成流式响应内容。

    Args:
        agent: CodeMindAgent 实例
        question: 用户问题
        docs: 检索到的文档
        summarizer_llm: 总结模型

    Yields:
        流式输出的内容片段
    """
    try:
        # 使用 Agent 的流式输出
        for chunk in agent.execute_stream(
            question=question,
            raw_docs=docs,
            summarizer_llm=summarizer_llm
        ):
            yield chunk
    except Exception as e:
        logger.error(f"Stream generation failed: {e}")
        yield f"\n[错误] 流式输出失败: {str(e)}\n"
```

### 3. /chat 接口：非流式输出（保持兼容）

```python
@app.post("/chat")
async def chat(query: Query):
    """Q&A endpoint with Agent support (non-streaming)."""
    vectordb = services.get("vectordb")
    llm = services.get("llm")
    summarizer_llm = services.get("summarizer_llm")
    retrieval_k = services.get("retrieval_k", 7)
    agent_enabled = services.get("agent_enabled", False)
    agent = services.get("agent")

    if vectordb is None or llm is None or summarizer_llm is None:
        return {"answer": "Service not initialized, please check if vector database exists"}

    logger.info(f"Received question: {query.question}")
    logger.info(f"Agent enabled: {agent_enabled}")

    # 1. 检索相关文档（这一步两种模式都一样）
    logger.info(f"开始检索向量库，检索数量: {retrieval_k}")
    docs = vectordb.similarity_search(query.question, k=retrieval_k)

    # Debug: 输出向量库检索完整结果
    logger.debug("=" * 80)
    logger.debug("向量库检索完整结果:")
    for i, doc in enumerate(docs):
        src = doc.metadata.get("source", "unknown")
        logger.debug(f"  [{i+1}] 来源: {src}")
        logger.debug(f"  内容:\n{doc.page_content}")
        logger.debug("-" * 60)
    logger.debug("=" * 80)

    # 分支：Agent 模式 or 普通模式？
    if agent_enabled and agent is not None:
        logger.info("Using Agent mode (non-streaming)...")
        result = agent.execute(
            question=query.question,
            raw_docs=docs,
            summarizer_llm=summarizer_llm
        )

        # Log sources (INFO level)
        logger.info("=" * 60)
        logger.info(" References (Agent mode):")
        for i, doc in enumerate(docs):
            src = doc.metadata.get("source", "unknown")
            snippet = doc.page_content[:150].replace("\n", " ")
            logger.info(f"  [{i+1}] {src}")
            logger.info(f"       Snippet: {snippet}...")
        logger.info("=" * 60)

        return {
            "answer": result.get("answer", ""),
            "sources": result.get("sources", []),
            "agent_mode": True,
            "summarized_context": result.get("summarized_context", ""),
            "error": result.get("error")
        }
    else:
        # 普通模式（Agent 禁用）
        logger.info("Using original mode (Agent disabled)...")

        # 2. 构建原始上下文
        raw_context = build_context(docs)

        # Debug: Log full raw context
        logger.debug("=" * 80)
        logger.debug("完整参考材料 (raw_context):")
        logger.debug(raw_context)
        logger.debug("=" * 80)

        # 3. 总结原始上下文
        logger.info("Summarizing retrieved documents...")
        summarized_context = summarize_context(query.question, raw_context, summarizer_llm)

        # Debug: Log summarized context
        logger.debug("=" * 80)
        logger.debug("总结后的上下文 (summarized_context):")
        logger.debug(summarized_context)
        logger.debug("=" * 80)

        # 4. 构建最终 Prompt
        prompt = PROMPT_TEMPLATE.format(context=summarized_context, question=query.question)

        # Debug: 输出最终提示词
        logger.debug("=" * 80)
        logger.debug("发送给主模型的最终提示词:")
        logger.debug(prompt)
        logger.debug("=" * 80)

        # 5. 调用主 LLM
        logger.info("正在调用主 LLM...")
        response = llm.invoke(prompt)
        answer = response.content if hasattr(response, 'content') else str(response)

        # Debug: 输出模型返回结果
        logger.debug("=" * 80)
        logger.debug("主模型返回结果:")
        logger.debug(answer)
        logger.debug("=" * 80)

        # Log sources (INFO level)
        logger.info("=" * 60)
        logger.info(" References:")
        for i, doc in enumerate(docs):
            src = doc.metadata.get("source", "unknown")
            snippet = doc.page_content[:150].replace("\n", " ")
            logger.info(f"  [{i+1}] {src}")
            logger.info(f"       Snippet: {snippet}...")
        logger.info("=" * 60)

        return {
            "answer": answer,
            "sources": [
                {
                    "source": doc.metadata.get("source", "unknown"),
                    "content": doc.page_content
                }
                for doc in docs
            ],
            "agent_mode": False,
            "summarized_context": summarized_context
        }
```

### 4. 新增：/chat/stream 接口 - 流式输出

```python
@app.post("/chat/stream")
async def chat_stream(query: Query):
    """Q&A endpoint with Agent support (streaming)."""
    vectordb = services.get("vectordb")
    summarizer_llm = services.get("summarizer_llm")
    retrieval_k = services.get("retrieval_k", 7)
    agent_enabled = services.get("agent_enabled", False)
    agent = services.get("agent")

    if vectordb is None or summarizer_llm is None:
        return StreamingResponse(
            iter(["Service not initialized\n"]),
            media_type="text/plain"
        )

    logger.info(f"Received streaming question: {query.question}")
    logger.info(f"Agent enabled: {agent_enabled}")

    # 1. 检索相关文档
    logger.info(f"开始检索向量库，检索数量: {retrieval_k}")
    docs = vectordb.similarity_search(query.question, k=retrieval_k)

    # Debug: 输出向量库检索完整结果
    logger.debug("=" * 80)
    logger.debug("向量库检索完整结果:")
    for i, doc in enumerate(docs):
        src = doc.metadata.get("source", "unknown")
        logger.debug(f"  [{i+1}] 来源: {src}")
        logger.debug(f"  内容:\n{doc.page_content}")
        logger.debug("-" * 60)
    logger.debug("=" * 80)

    # Log sources (INFO level)
    logger.info("=" * 60)
    logger.info(" References (streaming):")
    for i, doc in enumerate(docs):
        src = doc.metadata.get("source", "unknown")
        snippet = doc.page_content[:150].replace("\n", " ")
        logger.info(f"  [{i+1}] {src}")
        logger.info(f"       Snippet: {snippet}...")
    logger.info("=" * 60)

    # 2. Check if Agent should be used
    if agent_enabled and agent is not None:
        logger.info("Using Agent mode (streaming)...")
        return StreamingResponse(
            generate_stream(agent, query.question, docs, summarizer_llm),
            media_type="text/plain"
        )
    else:
        # Fallback to original mode (non-Agent, non-streaming for simplicity)
        logger.info("Agent not available, using non-streaming fallback...")

        # Build context and get answer
        raw_context = build_context(docs)
        summarized_context = summarize_context(query.question, raw_context, summarizer_llm)
        prompt = PROMPT_TEMPLATE.format(context=summarized_context, question=query.question)

        from langchain_community.chat_models import ChatTongyi
        llm = ChatTongyi(
            model=Config.get("llm.model", "qwen-max"),
            temperature=Config.get("llm.temperature", 0.1),
        )
        response = llm.invoke(prompt)
        answer = response.content if hasattr(response, 'content') else str(response)

        return StreamingResponse(
            iter([answer]),
            media_type="text/plain"
        )
```

---

## 三、日志配置详解

### 日志文件按天命名

日志配置已更新，日志文件现在按天命名：

```python
# 获取当前日期，格式：YYYY-MM-DD
today_str = datetime.now().strftime("%Y-%m-%d")
log_filename = f"app_{today_str}.log"
```

这样每天都会生成一个新的日志文件：
- `app_2026-04-16.log`
- `app_2026-04-17.log`
- `app_2026-04-18.log`

### 日志级别说明

| 级别 | 说明 | 输出位置 |
|------|------|---------|
| **DEBUG** | 详细调试信息（完整提示词、模型返回等） | 仅日志文件 `logs/app_YYYY-MM-DD.log` |
| **INFO** | 关键流程信息（工具调用、最终答案等） | 控制台 + 日志文件 |

### DEBUG 级别日志输出内容

```python
# 向量库检索完整结果
logger.debug("向量库检索完整结果:")

# 发送给 Agent 的用户消息
logger.debug("发送给 Agent 的用户消息:")

# Agent 完整返回结果
logger.debug("Agent 完整返回结果:")

# 发送给主模型的最终提示词（普通模式）
logger.debug("发送给主模型的最终提示词:")

# 主模型返回结果（普通模式）
logger.debug("主模型返回结果:")
```

### INFO 级别日志输出内容

```python
# 工具调用
logger.info(f"ReadFile called: file_path={file_path}")
logger.info(f"SearchCode called: query={query}")
logger.info(f"RunCommand called: {command}")

# Agent 最终答案
logger.info("Agent 最终答案:")

# 引用来源
logger.info(" References:")
```

---

## 四、完整测试指南

### 1. 准备工作

确保你已经：
- 安装了所有依赖：`pip install -r requirements.txt`
- 配置了 `.env` 文件（包含 `DASHSCOPE_API_KEY`）
- 索引了代码仓库：`python scripts/index_repo.py .`

### 2. 启动服务

```bash
python app.py
```

或

```bash
uvicorn app:app --reload
```

### 3. 测试非流式接口

打开浏览器访问：http://localhost:8000/docs

点击 `/chat` → "Try it out"，输入：

```json
{
  "question": "这个项目的安全模块是怎么实现的？"
}
```

点击 "Execute"，观察控制台日志输出！

### 4. 测试流式接口

点击 `/chat/stream` → "Try it out"，输入：

```json
{
  "question": "这个项目的 Agent 模块是怎么实现的？"
}
```

点击 "Execute"，你会看到响应是实时流式输出的！

### 5. 使用 curl 测试流式接口

```bash
curl -N -X POST "http://localhost:8000/chat/stream" \
  -H "Content-Type: application/json" \
  -d '{"question": "介绍一下项目结构"}'
```

### 6. 查看日志

DEBUG 级别的日志只输出到文件：

```bash
# Linux/Mac
tail -f logs/app_$(date +%Y-%m-%d).log

# Windows PowerShell
Get-Content "logs\app_$(Get-Date -Format 'yyyy-MM-dd').log" -Wait
```

### 7. 测试普通模式

在 `config.yml` 中设置：

```yaml
agent:
  enabled: false
```

重启服务，再次测试，对比两种模式的区别！

---

## 五、常见问题排查

### Q: Agent 初始化失败怎么办？

**A**: 检查日志中的错误信息，常见原因：
- 模型 API Key 无效
- 网络连接问题
- LangChain 版本不兼容

系统会自动禁用 Agent，回退到普通模式，不用担心服务挂掉。

### Q: 如何知道 Agent 是否真的被调用了？

**A**: 检查两点：
1. API 返回中有 `"agent_mode": true`
2. 控制台日志中有 `Using Agent mode...`

### Q: Agent 调用工具时会有日志吗？

**A**: 有的！每次工具调用都会在 INFO 级别输出：
```
ReadFile called: file_path=utils/logger.py
Successfully read file: utils/logger.py
```

### Q: 流式输出和非流式输出有什么区别？

**A**:

| 特性 | 非流式（/chat） | 流式（/chat/stream） |
|------|----------------|-------------------|
| **返回方式** | 一次性返回完整 JSON | 实时流式返回文本 |
| **用户体验** | 需要等待完整响应 | 可以边看边生成 |
| **适用场景** | API 调用、自动化测试 | 网页聊天、实时交互 |
| **返回格式** | JSON（含 answer、sources 等） | 纯文本流 |

---

## 六、两种模式的对比

| 特性 | Agent 模式 | 普通模式 |
|------|-----------|---------|
| **能否主动获取信息** | 可以调用工具 | 只能用检索结果 |
| **回答深度** | 更深，可以探索更多文件 | 较浅，依赖检索质量 |
| **Token 消耗** | 较高（多次工具调用） | 较低（一次 LLM 调用） |
| **速度** | 较慢（工具调用需要时间） | 较快 |
| **适用场景** | 复杂问题、需要多文件分析 | 简单问题、答案在检索结果中 |

---

## 恭喜！

你已经完成了 Phase02 的学习！现在你掌握了：
- Agent 的原理和 ReAct 模式
- 如何设计安全检查机制
- 如何使用 LangChain @tool 装饰器
- LangChain 1.0 的新 Agent API
- CodeMindAgent 类的使用
- 流式输出和非流式输出
- 如何集成 Agent 到 FastAPI
- 如何测试和调试 Agent
- 日志按天命名配置

继续探索，你可以尝试：
- 添加新的工具（比如 Git 历史查询、代码统计等）
- 优化 Agent 的提示词
- 添加记忆功能，让 Agent 记住之前的对话
- 实现更复杂的流式接口（返回 JSON 而非纯文本）
