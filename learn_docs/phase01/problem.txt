以下问题已解决=====================================================
1. .env是否需要排除？
3. 当按函数切分后，是否把完整的类也切开了
4. 从langchain_chroma中引入Chroma，不要从langchain中引入
5. 日志等工具放入utils文件夹下（记得防止重复初始化）
6. 配置类信息放入config.yml中，.env只放环境变量
7. @app.on_event("startup")： on_event is deprecated, use lifespan event handlers instead.
8. 提示词使用PromptTemplate,并优化提示词


以下是我的想法，你看看可行性，如果可行优化后执行，如果不可行告诉我
我的想法：
1. 对于3,能否先匹配类，再匹配函数进行切分？对于特别长的类（比如大于3000字符，只是举例）再考虑切分


为本项目增加新的一层：总结层
具体调整为：
将向量数据库搜索返回条数增加到10条，并在得到数据后将数据传递给总结模型，由总结模型总结后再将总结后的资料传递给agent使用

总结提示词模板如下，可进行参考（"""包围的段落）：
"""
你是专注于"基于参考资料总结"的AI助手，需结合用户提问和向量检索到的参考资料，生成简洁准确的概括回答。

### 输入信息
1. 用户提问：{input}
2. 参考资料(在下一个###之前内容均为参考资料)：{context}

### 严格遵守以下约束（违反将导致回答无效）
1. 内容合规：禁止包含违法、侵权、攻击性信息；
2. 事实准确：回答必须完全基于参考资料中的信息，不编造、不添加未提及的内容，不做主观推断；
3. 语言要求：仅用中文回答，语气客观、简洁，不冗余；
4. 聚焦提问：严格围绕用户原始提问总结，不扩充问题范围、不额外追问、不构造新query；
5. 格式要求：仅输出概括内容本身，以纯文本字符串形式呈现，不封装为字典、列表、JSON等任何结构，不附带额外说明。
"""
==================================================================








